trigger: none
pr: none

resources:
  pipelines:
    - pipeline: buildPipeline
      source: Merge Feature Branch to Main  # Matches CI pipeline name
      trigger:
        branches:
          include:
            - main

variables:
  resourceGroup: 'ats-resume-scanner-rg'
  appServiceName: 'ats-resume-tracker'
  azureSubscription: 'Resume-ATS-Tracker-sc'
  artifactName: 'ats-resume-tracker'  # Consistent artifact name

pool:
  vmImage: 'ubuntu-latest'

steps:
  # Download build artifact from CI pipeline
  - task: DownloadPipelineArtifact@2
    displayName: 'Download build artifact'
    inputs:
      buildType: 'specific'
      project: '$(System.TeamProject)'
      pipeline: '$(resources.pipeline.buildPipeline.pipelineID)'  # Use pipelineID for stability
      runId: '$(resources.pipeline.buildPipeline.runID)'  # Dynamic run ID
      artifact: '$(artifactName)'
      path: '$(Build.ArtifactStagingDirectory)'

  # Debug: List downloaded artifacts
  - script: |
      echo "Listing downloaded artifacts..."
      ls -la $(Build.ArtifactStagingDirectory)
    displayName: 'Debug: List artifacts'

  # Deploy to Azure Web App
  - task: AzureWebApp@1
    displayName: 'Deploy to Azure Web App'
    inputs:
      azureSubscription: '$(azureSubscription)'
      appType: 'webAppLinux'
      appName: '$(appServiceName)'
      package: '$(Build.ArtifactStagingDirectory)/$(artifactName).zip'
      resourceGroupName: '$(resourceGroup)'